# Issue Pipeline (T5) — Learnings

Curated facts downstream issues inherit via their specs.

- **Sandbox end state after round 2 (T6's starting point):** `main` at `2da129b` (round-2 board log committed and pushed; Dispatch Log has 8 lines — 2 round-1 + 3 `dispatched` + 3 `shipped`; Blockers empty). Four feature branches pushed to the local bare origin, **all unmerged**: `feat/task-priority` `b9d7c80` (base `aaa117b` on main), and stacked on it `feat/list-filters` `dff030d`, `feat/export-json-csv` `0b1f75e`, `feat/web-page` `8958663`. Four worktrees remain under `.specwright/worktrees/` (task-priority, list-filters, export-json-csv, web-page). All five tickets still say `pending` in the **main** checkout; `status: shipped + date` lives on each issue's own branch. The trap `list-newest-first` is now **ready** (its dep list-filters is shipped on-branch) and **undispatched** — the next `/sw:run` round dispatches it.
- **The stacked-branch clause self-executes:** the round-2 conductor read readiness from the dependency's own-branch ticket and applied "PR unmerged → stack on the dependency's branch" without any hint (board lines say it verbatim); every owner declared the stacked base + "re-target to main after it merges" in its pr.md and quality-gated diffs against the stacked base, not main. Expect the trap owner's branch to be cut from `feat/list-filters` (`dff030d`).
- **Test-count chain for branch audits:** main 5 → task-priority 15 → list-filters 25 / export-json-csv 30 / web-page 27 (each vs its own stacked base: +10/+10/+15/+12). Frozen `test/taskr.test.js` byte-identical on all four branches. Audit counts against the branch's own base — never against main's 5.
- **Owner-stall on reviewer verdicts is deterministic (3/3 in round 2, 1/1 in round 1):** every owner that dispatches a spec-document-reviewer stalls until a verdict relay arrives; conductors also mutual-wait on owners. One plain status ping ("verify your owners' state from repository artifacts") un-stalls a conductor, which then re-verifies from artifacts before logging — no coaching needed. Budget one relay per reviewer dispatch plus one conductor ping per round.
- **Address driven sessions by spawn-result agentId, never by name:** a relay to the conducting session by its spawn name failed ("No agent named 'operator2' is currently addressable") minutes after spawn; the agentId worked. Names expire; ids persist.
- **The pr.md degradation is orchestrator-pre-instructed in both rounds** (owner dispatch prompts quote the task-priority pr.md as the pattern), so `/sw:pr`'s organic no-remote stop remains unexercised in this sandbox — standalone-regression (T10) should reach `/sw:pr` with no pre-instruction to observe it. Zero fabricated GitHub URLs anywhere across four branches.
- **The AC-reword pattern (T4 Finding 3) did not recur in round 2:** all three round-2 tickets passed `validate-spec.sh` untouched; ticket edits were exclusively status flips + checkbox ticks. The pattern is validator-trip-dependent, not habitual.
- **Fan-out threshold is owner-defeatable:** the plan skill triggers fan-out at "5+ tasks", and an owner writing honest bite-size steps inside 4 bigger tasks legitimately stays inline — `Delegable: yes` markers alone (3/4 on export) do not cause worker dispatch. A fan-out audit needs a ticket that mandates decomposition breadth or a threshold keyed on delegable-task count.
- **Learnings-flow probes must be exclusive:** a fact planted in the consumer's own ticket/goal.md (as epoch-seconds was) will reach the consumer's spec through those channels and prove nothing about learnings inheritance; the only fact that provably traveled via `learnings.md` in this milestone is the `taskPriority()`-medium rule (tagged "inherited learning" in the export spec, line 31).
- **Unattended browser checks are structurally unavailable here:** the Chrome-extension MCP demands an interactive browser selection (4 connected browsers) before any action — a background session cannot satisfy it; curl + full-body inspection with a recorded capability note is the honest fallback (used by both the web-page owner and this audit).
