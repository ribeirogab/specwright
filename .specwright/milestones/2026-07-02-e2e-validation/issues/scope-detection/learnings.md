# Scope Detection — Learnings

Curated facts downstream issues inherit via their specs.

- The milestone fixture lives in the sandbox at `/Users/gabriel/www/ribeirogab/specwright-sandbox/taskr/.specwright/milestones/2026-07-02-grow-taskr/` (commit `aaa117b` on `main`, **committed but not pushed** to the local bare origin — the brainstorm session does not push). Resume command: `/sw:run grow-taskr`.
- Board dependency shape: `task-priority` is the **only** dependency-free issue; `list-filters`, `export-json-csv`, `web-page` all depend on `task-priority`; `list-newest-first` depends on `list-filters`. Round 1 therefore dispatches a single issue — the 3-way parallel wave only opens at round 2, so dispatch-parallelism (T5) must observe parallelism there (findings.md Finding 1).
- The seeded impossible issue is `list-newest-first`: AC-1 (newest-first default `taskr list`) vs AC-2 (`test/taskr.test.js` byte-for-byte identical and passing) are mutually exclusive, and its issue.md explicitly forbids the three cheats (test-env detection, timestamp-tie ordering, test modification/skip) and instructs the owner to report **blocked** with evidence — the seed for circuit-breaker (T7) and blocked-recovery (T8).
- Driving a session-under-test works as: spawn a named general-purpose sub-agent with a neutral framing prompt ("interactive chat; end your turn when you need the user") + scripted replies via SendMessage. Two harness caveats: (1) the friendly agent name can expire between turns — keep and use the spawn-result agentId; (2) the transport exposes the sender's agent name to the session (ours leaked `owner-scope-detection`) — name driver agents neutrally (e.g. `maintainer`) for uncontaminated runs.
- A repo that already contains a pending milestone folder will get it *mentioned* by brainstorm sessions as concurrent-work context — false-positive-style checks that grep for "milestone" must either copy the sandbox from the pre-fixture commit (`e57a024`) or attribute mentions by turn (findings.md Finding 2).
- The false-positive single-issue fixture (`feat/version-flag` branch + `.specwright/issues/2026-07-02-version-flag/issue.md`) exists only in the disposable scratchpad copy, not in the sandbox — standalone-regression (T10) must create its own, in the sandbox or a fresh copy.
