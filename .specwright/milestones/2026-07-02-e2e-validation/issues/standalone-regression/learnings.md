# Standalone Regression (T9) — Learnings

Curated facts downstream issues (and the fixes brainstorm) inherit via their specs.

- **Sandbox end state after T9 (final state of the e2e-validation milestone):** `main` unchanged at `ab65ca0` == `origin/main`, tree clean. **Six** feature branches pushed and unmerged: the five grow-taskr branches (tips unchanged from T8) plus the new standalone `feat/help-flag` @ `2dcc275` (history: issue `5b163f2` → impl `208d3f4`/`e01760a` → docs `f53c426` → ship `2dcc275`). **Six** worktrees under `.specwright/worktrees/` (grow-taskr's five + `help-flag`). The standalone vault `.specwright/issues/2026-07-02-help-flag/` (issue/spec/tasks/learnings, `status: shipped` 2026-07-02) exists **on `feat/help-flag` only** — the main checkout's `.specwright/issues/` still holds just `.gitkeep`. Suite: `main` 5/5, `feat/help-flag` 9/9. No `design.md` anywhere; no new milestone folder. Only pending human action: merging the six branches.
- **The organic `/sw:pr` no-GitHub degradation is now exercised (dossier 6.4 closed):** with zero pre-instruction, the session pushed the branch, hit `gh pr create` → "none of the git remotes ... point to a known GitHub host", stopped, explained, offered the manual command, fabricated nothing — matching the skill's documented "stop and explain". Two gaps worth fixing: it probed with a live junk-payload `gh pr create` instead of checking `git remote -v` first, and the organic path writes no `pr.md`, so the runtime-verification/PR-body record has no durable home (findings.md Divergences 1–2).
- **Driven sessions do not trust relayed gate verdicts:** faced with a third-party relay of its reviewer's Approved, the session re-confirmed directly with its own subagent before closing the gate ("gate é gate"). Relays reliably un-stall (notification misrouting reproduced again, 7/7) but the session treats them as hearsay — design harnesses accordingly.
- **The relay pattern scales to a full conversation:** an entire brainstorm→approval→batch dialogue (4 driver replies) ran with the opening ask in the spawn prompt and every reply via fresh neutral `maintainer*` relay agents to the spawn-result agentId — zero name leaks, zero contamination, session behavior indistinguishable from a human-attended chat.
- **zsh multios breaks `2>&1`-piped stream assertions:** when runtime verification must attribute output to stdout vs stderr, use explicit per-stream redirection to files; the driven session discovered and applied this itself.
- **The standalone path has no regression after the milestone machinery:** single-issue scope concluded organically, three-part batch (branch + worktree + handoff) asked verbatim in one message, standalone vault path used, JIT ordering provable from git history, pipeline to `shipped` complete. The continue-in-session handoff variant is now exercised (T1 exercised the offer; this run took "continue").
