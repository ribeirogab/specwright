# Closeout (T8) — Learnings

Curated facts downstream issues inherit via their specs.

- **Sandbox end state after closeout (the baseline for standalone-regression):** grow-taskr is **fully closed out and the sandbox is idle**. `main` at `ab65ca0` (== `origin/main`; history: `5a9b369` → `d9c8114` final summary appended → `ab65ca0` maintainer decisions applied), working tree clean. Board: Issues table + the 13 Dispatch Log lines + Blockers `_None._` all byte-unchanged from T7; below them a `## Final Summary — 2026-07-02` section (per-issue delivery table, blocker history, stacked merge order, closeout items marked resolved). `goal.md` reconciled: its two `test/taskr.test.js` freeze bullets now carry an explicit exception for the two ordering assertions, citing the option-2 decision. `.specwright/conventions/store-and-constants.md` exists (the approved promotion subset: store-format invariants + vocabulary constants); `AGENTS.md` untouched (sha `f336a9a3…db27`). Five branches pushed, still unmerged, same tips (`feat/task-priority` `b9d7c80`, `feat/list-filters` `dff030d`, `feat/export-json-csv` `0b1f75e`, `feat/web-page` `8958663`, `feat/list-newest-first` `a15025c`); five worktrees remain; all five tickets still `pending` on `main` / `shipped` on-branch. Nothing is ready, nothing is running; the only pending human action is merging the five stacked PRs. A re-run of `/sw:run grow-taskr` should find a closed milestone and do nothing destructive.
- **The closeout contract executes end to end** — summary append (history byte-intact, strictly additive vs the pre-closeout board), promotion proposal read from all five issues' `learnings.md` (7 families, including epoch-seconds), hard stop for maintainer approval, exactly-the-approved-subset application with rejected items left as issue-folder history, and the final report handing merges back. One skill-text gap: the goal.md reconciliation the milestone *needed* was proposed and approval-gated by the conductor with no textual basis in the run skill (scope guard says "never edits `goal.md`" with no closeout exception) — dossier entry 1.6/9.1; a stricter conductor could have refused and left the goal stale.
- **Closeout writes the summary before the approval gate, then amends its own "open items" subsection after the decisions** — the diff vs the pre-closeout board stays strictly additive (all edits land inside the section closeout itself appended), so freeze audits must diff against the pre-closeout copy, not against the first summary commit.
- **Conversational decisions reach driven sessions when (a) the spawn prompt pre-authorizes messages in the conversation as maintainer answers and (b) a NEUTRAL relay delivers them to the spawn-result agentId.** The message arrived as `<agent-message from="maintainer8">` — no slug leak; the conductor applied the decisions verbatim. This is the accepting-side boundary of T4's categorical refusal (which concerns pending permission prompts). Bonus data point: the conductor's reply to the relay was delivered while the relay was still live — alias expiry is a race, not a law; keep treating relays as one-way anyway.
- **The consolidated dossier lives at this issue folder's `dossier.md`:** ~30 deduplicated entries across 9 themes with severity, full evidence trails for the recurring patterns (name-alias expiry, owner-stall 6/6, notification routing, approval-in-spawn-prompt, name leak ×4, verbatim-vs-synthesis), the two user-requested run-skill improvements (progress panel; stall-proof/watchdog) as first-class items, and a recommended fixes decomposition: **a 5-issue milestone** (run-skill-contract, run-skill-progress-panel, plan-skill-contract, brainstorm-and-templates, install-parity-and-docs). standalone-regression's findings were not yet written at consolidation time — append them at the fixes brainstorm.
- **For standalone-regression** (T-labels drift across sibling files — map by slug, per T4's findings note)**:** the organic `/sw:pr` no-remote stop is still unexercised anywhere (dossier 6.4) — reach `/sw:pr` with no pre-instruction; the false-positive single-issue fixture does not exist in the sandbox (T1 learning) — create your own; the sandbox now has a real convention file (`store-and-constants.md`), so a standalone issue's plan gates can exercise conventions-compliance against it.
