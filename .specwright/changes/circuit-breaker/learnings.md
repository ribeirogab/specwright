# Circuit Breaker (T6) — Learnings

Curated facts downstream issues inherit via their specs.

- **Sandbox end state after the trap round + halt run (T7's starting point):** `main` at `d26c321` (Dispatch Log 10 lines — 8 inherited + trap `dispatched` + trap `blocked`; Blockers holds one entry: the `list-newest-first` report with Why/Tried/Needs). Five branches pushed, all unmerged: `feat/task-priority` `b9d7c80`, stacked `feat/list-filters` `dff030d` / `feat/export-json-csv` `0b1f75e` / `feat/web-page` `8958663`, and the trap `feat/list-newest-first` `c74e996` (stacked on `feat/list-filters`; commits plan `b840aba` → demo `f579b96` → revert `b843a94` → evidence `c74e996`; tree byte-identical to base except vault files). Five worktrees. Trap ticket: `status: blocked` on its branch/worktree, `pending` on `main` (the orchestrator never edits tickets); the other four tickets stay `shipped` on-branch / `pending` on main. The halt run changed **nothing** — it is re-runnable for verification.
- **The halt report T7 must verify lives at `evidence/halt-run-session.md`** (this issue's folder): `operator4`'s consolidated blockers report, captured verbatim, **including** the flip instruction — "Quando decidir, edite a issue conforme a opção escolhida, volte o `status:` para `pending` e rode `/sw:run` de novo." T7's recovery move per that report and the run skill: edit the trap's `issue.md` per the chosen option (drop AC-1, or lift the freeze on `test/taskr.test.js:35-42` + `test/list-filters.test.js:90-97`) in the checkout that carries `blocked` (worktree/branch copy), flip `status: pending`, re-run `/sw:run grow-taskr`. The reference implementation for option 2 is reusable as-is from commit `f579b96`.
- **Two tests, not one, pin the oldest-first default** — the brainstorm-era trap description assumed only the frozen `test/taskr.test.js:35`; round 2's list-filters added a second pin (`test/list-filters.test.js:90`, byte-identity assertion). Any unblock via option 2 must amend both; T7 should expect the owner it re-dispatches to hit both.
- **The blocked contract executes end to end but "verbatim" is aspirational:** conductors synthesize the Blockers entry from the owner's return + its evidence file (faithful, three parts, zero semantic loss — but not byte-copyable against any single artifact). Audits must check part-presence and fact-fidelity, not byte equality (findings.md Divergence 1).
- **An impossible AC does not trigger the reword pattern:** faced with a semantically unfixable criterion, the trap owner never touched the AC text — it demonstrated (demo commit → verbatim failures → revert → green), blocked, and left ACs unticked. T4's reword pattern remains validator-trip-specific.
- **The demonstrate-then-revert protocol is the efficient blocked path:** 1 gate failure total, ~16 min owner pipeline, branch ends green (25/25) and byte-identical to base — a blocked issue can still deliver a pushed branch + evidence with zero net code change. Expect exactly this shape when re-auditing blocked branches.
- **Name-alias expiry is bidirectional:** driver→driven needs the spawn agentId (known), and driven→relay replies fail too (`maintainer3` unaddressable at reply time). Treat every relay as one-way; read answers from repository artifacts.
- **Blockers-only resume is safe and idempotent:** a fresh conductor with a one-round mandate and zero ready issues appends nothing to the append-only log, dispatches nothing, and escalates with the consolidated report — verified once here; T7 can re-run it freely before flipping the status.
