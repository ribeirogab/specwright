# Blocked Recovery (T7) — Learnings

Curated facts downstream issues inherit via their specs.

- **Sandbox end state after the recovery round (T8's starting point):** grow-taskr is **5/5 shipped-on-branch**. `main` at `5a9b369` (Dispatch Log **13** lines — 10 inherited + `resumed` + `dispatched` + `shipped` for list-newest-first; **Blockers section is `_None._`** — the entry was deleted per the documented contract). Five branches pushed, **all unmerged**: `feat/task-priority` `b9d7c80`, stacked `feat/list-filters` `dff030d` / `feat/export-json-csv` `0b1f75e` / `feat/web-page` `8958663`, and `feat/list-newest-first` `a15025c` (stacked on `feat/list-filters`; full history: plan `b840aba` → demo `f579b96` → revert `b843a94` → evidence `c74e996` → maintainer unblock `1be3afa` → replan `8eb07ff` → impl `f377675` → readme `882d28e` → ship `a15025c`). Five worktrees remain. All five tickets still say `pending` on `main` (shipped status + the amended trap contract live on-branch only). Closeout has **not** run — the round-4 conductor hard-stopped and explicitly deferred it: final board summary, learnings promotion (maintainer approval), and `goal.md` reconciliation are T8's; merging the five stacked PRs is the human's.
- **The recovery contract executes end to end exactly as the halt report promises — when the edit lands on the blocked checkout:** amend the ticket + flip `status: pending` on the issue's own branch/worktree copy, re-run `/sw:run`, and an unhinted conductor (spawn prompt byte-identical to the halt run's) logs a `resumed` event citing the maintainer's commit, clears the Blockers entry unprompted, reuses the existing worktree/branch, and the re-dispatched owner replans against the amended ACs and ships. The board vocabulary's `resumed` event got its first real use.
- **The halt report's recovery line is checkout-ambiguous:** "edite a issue" names no path/branch and no commit instruction; only the branch/worktree copy can be flipped (`main`'s already says `pending` and is read by nobody in the loop). Editing `main` instead would silently do nothing. Proposed one-sentence fix to the run skill's escalation contract — charged to closeout (findings.md Divergence 1).
- **A seeded contradiction lives in every restatement, not just the AC:** unblocking the trap took 5 hunks (AC-2, Purpose, forbidden-resolutions bullet, Non-Goals bullet, + the decision note), because rescoping only the AC line would have left Purpose/forbidden-list contradictions for a strict owner to re-block on. Maintainer-edit guidance should say "sweep the ticket for restatements of the dropped constraint".
- **`goal.md` in the sandbox is now stale by decision:** it still says `test/taskr.test.js` stays byte-for-byte untouched, superseded for the two ordering assertions by the recorded option-2 decision (`1be3afa`). The round-4 owner flagged it on the board and in its pr.md review note; goal edits are a human scope change — T8 must reconcile it with maintainer approval, not silently.
- **Never SendMessage a session under test from a slug-named session:** T7's budgeted status ping went out directly and arrived as `from="owner-blocked-rec"` — fourth reproduction of the attribution leak. Timing made it harmless here (all recovery behaviors predated the ping). Spawn a neutral relay agent for every ping; promotion candidate at closeout.
- **Owner-stall is now 6/6 and the T6 ping text is a proven un-staller:** one plain "verify your owner's state from repository artifacts" message to the conductor's agentId produced independent artifact re-verification (it re-ran the suite itself) before logging — no coaching, second confirmation.
