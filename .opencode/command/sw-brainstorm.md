---
description: Design exploration that concludes single change vs delivery and writes the specwright artifacts — the required first step before any creative work
---

Run the specwright **brainstorm** skill. Load the skill named `brainstorm` from the
specwright plugin (`skills/brainstorm/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
