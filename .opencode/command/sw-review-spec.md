---
description: External evaluator that reviews a change's plan against the project conventions and the approved change, flagging vagueness, scope creep, and unresolved questions
---

Run the specwright **review-spec** skill. Load the skill named `review-spec` from the
specwright plugin (`skills/review-spec/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
