---
description: Set up specwright's project state — the .specwright/ vault, canonical AGENTS instructions, host adapter files, role profiles, and exact ignore rules
---

Run the specwright **init** skill. Load the skill named `init` from the
specwright plugin (`skills/init/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
