---
description: Turn intent into an approved change ticket — harvest the conversation, explore what is still open, decide one change or a delivery, and write proposal.md with binary acceptance criteria
argument-hint: "<optional: topic, or the current conversation if omitted>"
---

# propose — intent into an approved ticket

Run the specwright **propose** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/propose/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
