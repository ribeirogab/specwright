---
description: Conduct a specwright delivery — dispatch every ready change to the sw-change-owner role in parallel, track the board, apply circuit breakers, and close out. Resumable from a fresh session
argument-hint: "<optional: path to delivery, or the active delivery if omitted>"
---

# run — the delivery orchestrator

Run the specwright **run** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/run/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
