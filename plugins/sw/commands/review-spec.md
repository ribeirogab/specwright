---
description: External evaluator that reviews a change's plan against the project conventions and the approved change, flagging vagueness, scope creep, and unresolved questions
argument-hint: "<optional: path to change folder, or current change if omitted>"
---

# review-spec — external plan-review entry point

Run the specwright **review-spec** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/review-spec/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
