---
description: Open the current issue's pull request following specwright conventions — branch/base resolution, repo PR template, Conventional-Commit title, runtime-verification results, no AI attribution
argument-hint: "<optional: path to issue folder, or current issue if omitted>"
---

# pr — open the issue's pull request

Run the specwright **pr** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/pr/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
