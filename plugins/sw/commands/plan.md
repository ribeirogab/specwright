---
description: Produce an approved issue's fused spec.md + tasks.md, self-review them, then drive the issue pipeline — implement, quality gate, runtime verification, PR, review to lgtm
argument-hint: "<optional: path to issue folder, or current issue if omitted>"
---

# plan — the issue pipeline, from ticket to shipped

Run the specwright **plan** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/plan/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
