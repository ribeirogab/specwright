---
description: Conduct a large outcome decomposed into changes — decompose a requirements document if needed, dispatch every ready change to an owner in parallel, track the delivery, and close out. Resumable
argument-hint: "<optional: delivery slug, or a requirements document to decompose>"
---

# delivery — conduct many changes to done

Run the specwright **delivery** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/delivery/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
