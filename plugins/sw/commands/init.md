---
description: Set up specwright in this repository — the .specwright/ vault, the specwright section in the canonical AGENTS instructions, the Codex role profiles, and the ignore rules. Idempotent
argument-hint: "<optional: shared or local>"
---

# init — set up specwright in this repository

Run the specwright **init** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/init/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
