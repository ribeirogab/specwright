---
description: Run the whole change ladder without stopping — ticket, plan, implementation, quality gate, runtime verification, PR, review to lgtm — deciding open questions autonomously and recording each decision
argument-hint: "<optional: what to build, or the current conversation if omitted>"
---

# ship — the ladder, without stops

Run the specwright **ship** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/ship/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
