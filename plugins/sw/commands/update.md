---
description: Plan and apply a confirmed versioned specwright project migration across every supported host without overwriting drift
argument-hint: "<optional: shared or local>"
---

# update — migrate managed project state

Run the specwright **update** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/update/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
