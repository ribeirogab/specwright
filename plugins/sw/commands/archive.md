---
description: Close out a change whose branch already merged — confirm the merge from git alone, then file the change folder under changes/archive/ with its ship date
argument-hint: "<optional: change folder or slug, or every shipped change if omitted>"
---

# archive — close out a merged change

Run the specwright **archive** skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/archive/SKILL.md` and follow its instructions exactly, treating `$ARGUMENTS` as the skill's input.
