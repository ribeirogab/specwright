---
description: Plan and apply a confirmed versioned specwright project migration across every supported host without overwriting drift
---

Run the specwright **update** skill. Load the skill named `update` from the
specwright plugin (`skills/update/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
