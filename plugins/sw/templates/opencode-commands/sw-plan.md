---
description: Produce an approved change's fused spec.md + tasks.md, self-review them, then drive the change pipeline — implement, quality gate, runtime verification, PR, review to lgtm
---

Run the specwright **plan** skill. Load the skill named `plan` from the
specwright plugin (`skills/plan/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
