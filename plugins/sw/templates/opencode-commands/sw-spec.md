---
description: Turn the current conversation into a change (or delivery) using the specwright brainstorm flow
---

Run the specwright **spec** skill. Load the skill named `spec` from the
specwright plugin (`skills/spec/SKILL.md`) and follow its instructions exactly,
treating `$ARGUMENTS` as the skill's input.
